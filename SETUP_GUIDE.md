# 🚀 Практическая инструкция: Настройка Gemini-API в Docker

## 📝 Шаг 1: Получение cookies из браузера

### Метод 1: Через DevTools (рекомендуется)

1. **Откройте** https://gemini.google.com в Chrome/Firefox
2. **Залогиньтесь** под Google аккаунтом
3. Нажмите **F12** (откроется DevTools)
4. Перейдите на вкладку **Network**
5. Нажмите **F5** (обновить страницу)
6. Кликните на **любой запрос** в списке
7. Найдите раздел **Request Headers** → **Cookie**
8. Скопируйте значения:

```
__Secure-1PSID=значение_начинается_с_букв_и_цифр.длинная_строка
__Secure-1PSIDTS=sidts-другая_длинная_строка
```

**Важно:** Копируйте ТОЛЬКО значение после `=`, без названия cookie!

### Метод 2: Через расширение браузера

Используйте расширение [EditThisCookie](https://chrome.google.com/webstore/detail/editthiscookie) (Chrome) или Cookie-Editor (Firefox):
1. Откройте https://gemini.google.com
2. Кликните на иконку расширения
3. Найдите `__Secure-1PSID` и `__Secure-1PSIDTS`
4. Скопируйте их значения

---

## ⚙️ Шаг 2: Создание .env файла

### Скопируйте шаблон
```bash
cd /projects/google-ai-studio-automation/Gemini-API
cp .env.example .env
```

### Отредактируйте .env

Откройте `.env` в любом редакторе:
```bash
nano .env
# или
vim .env
# или
code .env
```

---

## 🔑 Шаг 3: Заполнение .env

### Минимальная конфигурация (обязательно):

```bash
# ============================================
# ОБЯЗАТЕЛЬНЫЕ credentials
# ============================================
GEMINI_PSID=g.a000lwi...ваш_длинный_psid...xyz
GEMINI_PSIDTS=sidts-CjEB...ваш_длинный_psidts...xyz

# ============================================
# Режим работы
# ============================================
MODE=api
# cli  - простой скрипт (для тестирования)
# api  - HTTP сервер (для продакшена)
```

### Полная конфигурация (с опциями):

```bash
# ============================================
# Gemini API Credentials
# ============================================
GEMINI_PSID=g.a000lwi...ваш_psid
GEMINI_PSIDTS=sidts-CjEB...ваш_psidts

# ============================================
# Proxy (если нужен)
# ============================================
# Формат: http://user:pass@ip:port
# GEMINI_PROXY=http://proxy.example.com:8080

# ============================================
# Режим работы
# ============================================
MODE=api

# ============================================
# API настройки (для MODE=api)
# ============================================
API_PORT=8000
API_HOST=0.0.0.0

# ============================================
# Gemini настройки
# ============================================
GEMINI_TIMEOUT=30
GEMINI_AUTO_REFRESH=true
GEMINI_REFRESH_INTERVAL=540

# ============================================
# Логирование
# ============================================
LOG_LEVEL=INFO
# Варианты: DEBUG, INFO, WARNING, ERROR, CRITICAL
```

---

## 🌐 Шаг 4: Настройка Reverse Proxy (Dockploy/Traefik)

Судя по скриншоту, у тебя Dockploy. Вот правильные настройки:

![Dockploy Settings](file:///home/inves/.gemini/antigravity/brain/b21159f9-226d-4614-9e45-6304aa45402f/uploaded_image_1764787512476.png)

### Конфигурация Dockploy:

| Поле | Значение | Пояснение |
|------|----------|-----------|
| **Service Name** | `gemini-api` (выбери из списка) | Имя сервиса из docker-compose.yml |
| **Host** | `api.dokploy.com` | Твой домен для доступа к API |
| **Path** | `/` | Внешний путь (URL) |
| **Internal Path** | `/` | Путь внутри контейнера |
| **Strip Path** | `OFF` | Не убирать путь из запроса |
| **Container Port** | `8000` | Порт из docker-compose.yml |
| **HTTPS** | `ON` | Автоматический SSL сертификат |

### В docker-compose.yml уже настроено:

```yaml
ports:
  - "8000:8000"  # Порт который слушает Dockploy
```

### Проверка после деплоя:

```bash
# Health check
curl https://api.dokploy.com/health

# Тестовый запрос
curl -X POST https://api.dokploy.com/ask \
  -H "Content-Type: application/json" \
  -d '{"prompt": "Привет!"}'
```

---

## 🔐 Где прописывать прокси (если нужен)?

### Вариант 1: В .env файле (рекомендуется)

```bash
GEMINI_PROXY=http://username:password@proxy-ip:port
```

**Примеры:**
```bash
# HTTP прокси без авторизации
GEMINI_PROXY=http://45.67.89.123:8080

# HTTP прокси с авторизацией
GEMINI_PROXY=http://user:pass@45.67.89.123:8080

# SOCKS5 прокси
GEMINI_PROXY=socks5://45.67.89.123:1080
```

### Вариант 2: В docker-compose.yml (для всех контейнеров)

```yaml
services:
  gemini-api:
    environment:
      HTTP_PROXY: http://proxy-ip:port
      HTTPS_PROXY: http://proxy-ip:port
      NO_PROXY: localhost,127.0.0.1
```

---

## 🎯 Полный workflow для Dockploy

### 1. Подготовка на локальной машине

```bash
# Клонируй репо (если ещё не сделал)
cd /projects/google-ai-studio-automation/Gemini-API
git checkout feature/docker-integration

# Создай .env файл
cp .env.example .env
nano .env  # Вставь свои cookies
```

### 2. Деплой в Dockploy

#### Через GitHub (автоматически):
1. Пуш в GitHub: `git push origin feature/docker-integration`
2. В Dockploy: Create Project → GitHub → выбери репо
3. Укажи ветку: `feature/docker-integration`
4. Dockploy автоматически:
   - Обнаружит `docker-compose.yml`
   - Соберёт образ
   - Запустит контейнер

#### Вручную через .env:
1. В Dockploy → Settings → Environment Variables
2. Добавь переменные:
   ```
   GEMINI_PSID=твой_psid
   GEMINI_PSIDTS=твой_psidts
   MODE=api
   ```

### 3. Настройка Domain (см. скриншот)
- Host: `api.dokploy.com` (или твой домен)
- Container Port: `8000`
- HTTPS: ON
- Сохранить

### 4. Проверка работы

```bash
# Health check
curl https://api.dokploy.com/health

# Первый запрос
curl -X POST https://api.dokploy.com/ask \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "Расскажи анекдот про программистов",
    "model": "gemini-2.5-flash"
  }'
```

---

## 🛡️ Безопасность .env файла

### ⚠️ НИКОГДА не коммить .env в Git!

Проверь `.gitignore`:
```bash
cat .gitignore | grep .env
```

Должно быть:
```
.env
.env.local
```

### Для Dockploy используй:
- **Environment Variables** в UI Dockploy
- **Docker Secrets** для production
- **Vault** для enterprise

---

## 📊 Примеры запросов к API

### Простой запрос
```bash
curl -X POST https://api.dokploy.com/ask \
  -H "Content-Type: application/json" \
  -d '{"prompt": "Привет!"}'
```

### С выбором модели
```bash
curl -X POST https://api.dokploy.com/ask \
  -d '{
    "prompt": "Сгенерируй код на Python",
    "model": "gemini-2.5-pro"
  }'
```

### Из Python
```python
import requests

response = requests.post(
    "https://api.dokploy.com/ask",
    json={"prompt": "Расскажи анекдот"}
)

print(response.json()["text"])
```

---

## 🐛 Troubleshooting

### Проблема: "Services not found" в Dockploy

**Причина:** Контейнер не запущен

**Решение:**
```bash
# Проверь логи
docker logs gemini-api

# Перезапусти
docker-compose restart
```

### Проблема: "Failed to initialize client"

**Причина:** Неверные cookies

**Решение:**
1. Получи новые cookies из браузера
2. Обнови `.env` или Environment Variables в Dockploy
3. Перезапусти: `docker-compose restart`

---

## ✅ Чеклист перед запуском

- [ ] Получены `__Secure-1PSID` и `__Secure-1PSIDTS` из браузера
- [ ] Создан `.env` файл с правильными значениями
- [ ] `MODE=api` установлен
- [ ] В Dockploy настроен Service Name и Container Port (8000)
- [ ] Домен прописан в Host
- [ ] HTTPS включен
- [ ] Health check возвращает `{"status": "healthy"}`

Готово! 🚀
